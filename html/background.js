function setup(){
    createCanvas(window.innerWidth,window.innerHeight)
}

function draw(){
    background(50)
    loadPixels();
    for (var y = 0; y < window.innerHeight; y++){
        for (var x = 0; x < window.innerWidth; x++){
            var index = 0

            
        }
    }
    for (var test=0;test<50000;test++){
        pixels[test] = 255;
    }
}
